interface Worker{
    //for an interface, it only declares and not define the functions and for all that implement the interface class they must define the functions.
    void performDuties();
    void workerDetails();
}